import { PageShell } from "@/components/layout/page-shell";

export default function SettingsPage() {
  return (
    <PageShell
      eyebrow="Account"
      title="Settings"
      description="Profile, sessions, preferences, and safety controls will be managed here."
    />
  );
}
