import { PageShell } from "@/components/layout/page-shell";

export default function CharacterDetailPage() {
  return (
    <PageShell
      eyebrow="Character"
      title="Character profile"
      description="Biography, journals, memories, relationships, and scene history will meet here."
    />
  );
}
