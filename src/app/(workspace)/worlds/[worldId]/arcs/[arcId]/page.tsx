import { PageShell } from "@/components/layout/page-shell";

export default function StoryArcDetailPage() {
  return (
    <PageShell
      eyebrow="Arc"
      title="Arc overview"
      description="Arc summaries, scene order, and progress will live in this view."
    />
  );
}
