import { PageShell } from "@/components/layout/page-shell";

export default function StoryArcsPage() {
  return (
    <PageShell
      eyebrow="World"
      title="Story arcs"
      description="Scenes can be grouped into planned, active, completed, and archived arcs."
    />
  );
}
