import { PageShell } from "@/components/layout/page-shell";

export default function ProfilePage() {
  return (
    <PageShell
      eyebrow="Profile"
      title="Writer profile"
      description="Public profile details and shared worlds will be represented here."
    />
  );
}
