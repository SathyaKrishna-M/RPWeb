import { PageShell } from "@/components/layout/page-shell";

export default function NotificationsPage() {
  return (
    <PageShell
      eyebrow="Updates"
      title="Notifications"
      description="Scene turns, invitations, mentions, and moderation updates will appear here."
    />
  );
}
